import * as React from 'react';

import { Layout } from '../components/layout/Layout';

const IndexPage = () => {
  return (
    <Layout wrapperClass="page-home" pageHeader={<></>}>
      <></>
    </Layout>
  );
};

export default IndexPage;
